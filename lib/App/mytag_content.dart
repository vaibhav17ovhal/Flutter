import 'package:flutter/material.dart';

class TagsContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('Tags Content', style: TextStyle(color: Colors.white),);
  }
}